# Apache NimBLE 代码架构文档

## 1. 项目概述

Apache NimBLE 是 Apache 软件基金会旗下的开源 **Bluetooth 5.4 协议栈**（包含 Host 主机层与 Controller 控制器层），隶属于 Apache Mynewt 项目。它可以完全替代 Nordic 芯片上的专有 SoftDevice 协议栈。

**仓库名称**：`apache-mynewt-nimble`
**许可证**：Apache License 2.0
**当前版本**：0.0.0（master 开发分支，最新稳定版 1.10.0）

### 核心特性

- ✅ 支持 251 字节数据包大小
- ✅ 同时支持 4 种 BLE 角色：Broadcaster / Observer / Peripheral / Central
- ✅ 最多支持 32 路并发连接
- ✅ Legacy 与 SC（安全连接）SMP 配对与绑定
- ✅ 广播扩展（Advertising Extensions）
- ✅ 周期性广播（Periodic Advertising）
- ✅ Coded PHY（长距离）与 2M PHY
- ✅ **Bluetooth Mesh 组网协议**
- ✅ LE Audio / Auracast（蓝牙音频广播）
- ✅ 信道 sounding（CS）/ 室内测距

---

## 2. 整体架构分层

```
┌───────────────────────────────────────────────────────────────┐
│                     Application (apps/)                       │
│  bleprph / blecent / btshell / blemesh / auracast / ...      │
├───────────────────────────────────────────────────────────────┤
│                    Host Services (host/services/)             │
│  GAP / GATT / ANS / BAS / DIS / IAS / LLS / TPS / IPSS ...   │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│                   Host Stack (nimble/host/)                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │  GAP     │ │  GATT    │ │  SM      │ │  Mesh Subsystem  │  │
│  │(ble_gap) │ │(gatts/c) │ │(ble_sm)  │ │ (host/mesh/)     │  │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────────┬─────────┘  │
│       │            │            │                 │            │
│  ┌────┴────────────┴────────────┴─────────────────┴─────────┐  │
│  │              L2CAP (Logical Link Control)                │  │
│  │         ble_l2cap.c / ble_eatt (Enhanced ATT)            │  │
│  └────────────────────────┬─────────────────────────────────┘  │
│                           │                                    │
│  ┌────────────────────────┴─────────────────────────────────┐  │
│  │              HCI Host Interface (ble_hs_hci)             │  │
│  └────────────────────────┬─────────────────────────────────┘  │
│                                                               │
├────────────────────────────┼──────────────────────────────────┤
│                            │                                   │
│         Transport Layer (nimble/transport/)                   │
│  ┌────────┐ ┌────────┐ ┌──────────┐ ┌───────┐ ┌───────────┐  │
│  │  UART  │ │ Socket │ │  RAM     │ │ USB   │ │ emSPI     │  │
│  │ uart/  │ │socket/ │ │ (shared) │ │ usb/  │ │ emspi/    │  │
│  └────────┘ └────────┘ └──────────┘ └───────┘ └───────────┘  │
│                                                               │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│              Controller (nimble/controller/)                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │         Link Layer (ble_ll_*.c)                          │  │
│  │  Adv / Scan / Conn / ISO / Sync / Schedule / Crypto     │  │
│  └────────────────────────┬────────────────────────────────┘  │
│                           │                                    │
│  ┌────────────────────────┴────────────────────────────────┐  │
│  │          HCI Controller Interface (ble_ll_hci)           │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                               │
├───────────────────────────────────────────────────────────────┤
│              Radio Drivers (nimble/drivers/)                  │
│   nrf51 / nrf52 / nrf5340 / dialog_cmac / native / fem       │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│         NimBLE Porting Layer (NPL) - porting/npl/             │
│     mynewt / linux / freertos / nuttx / riot / dummy          │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

---

## 3. 顶层目录结构

```
mynewt-nimble/
├── nimble/              # 协议栈核心（最重要的目录）
│   ├── controller/      # BLE 控制器（Link Layer + HCI 控制端）
│   ├── drivers/         # 射频芯片驱动（nRF51/52/5340、Dialog CMAC 等）
│   ├── host/            # BLE 主机层（GAP/GATT/L2CAP/SM + Mesh）
│   ├── include/         # 公共头文件
│   ├── transport/       # Host ↔ Controller 之间的传输层
│   ├── pkg.yml          # Newt 构建系统包描述
│   └── syscfg.yml       # 系统配置默认值
│
├── porting/             # 跨 OS / 跨平台移植层
│   ├── npl/             # NimBLE Porting Layer（OS 抽象接口实现）
│   │   ├── mynewt/
│   │   ├── linux/
│   │   ├── freertos/
│   │   ├── nuttx/
│   │   ├── riot/
│   │   └── dummy/
│   ├── nimble/          # 通用移植辅助代码（内存、mbuf、定时器等）
│   ├── examples/        # 移植示例工程
│   └── targets/         # 移植构建目标
│
├── apps/                # 示例应用程序
│   ├── bleprph/         # 最小外设示例
│   ├── blecent/         # 中心设备示例
│   ├── blehci/          # 纯控制器 HCI 应用（对接 BlueZ 等）
│   ├── btshell/         # 交互式 Shell（可通过 CLI 调试大部分功能）
│   ├── blemesh/         # Mesh 节点示例
│   ├── blemesh_shell/   # Mesh Shell 示例
│   ├── blemesh_light/   # Mesh 灯光模型示例
│   ├── blehr/           # 心率服务
│   ├── blecsc/          # 骑行速度与踏频
│   ├── auracast/        # LE Audio Auracast 广播
│   ├── dtm/             # 直接测试模式
│   ├── advertiser/      # 广播器
│   ├── scanner/         # 扫描器
│   ├── bttester/        # 测试框架
│   └── ...
│
├── ext/                 # 外部依赖库
│   ├── tinycrypt/       # 轻量加密库
│   ├── liblc3/          # LC3 音频编解码器（LE Audio）
│   └── libsamplerate/   # 音频采样率转换
│
├── docs/                # 文档（Sphinx/RST 格式）
│   ├── index.rst
│   ├── mesh/            # Mesh 相关文档
│   ├── btshell/         # btshell 使用说明
│   └── ...
│
├── targets/             # Newt 构建目标（硬件平台）
│   ├── dialog_cmac/
│   ├── nordic_pca10056-blehci-usb/   # nRF52840 DK
│   ├── nordic_pca10095_net-blehci/   # nRF5340 DK
│   ├── auracast_usb/
│   └── unittest/
│
├── tools/               # 辅助工具
│   └── hci_throughput/  # HCI 吞吐量测试工具
│
├── projects/            # 工程配置（一般为空，由 Mynewt newt 工具生成）
├── qualification/       # BQB 蓝牙认证相关文件
├── babblesim/           # 仿真测试（BabbleSim 模拟器集成）
├── .github/             # CI/CD（GitHub Actions）
│
├── README.md
├── CODING_STANDARDS.md  # 代码规范
├── RELEASE_NOTES.md
├── LICENSE              # Apache 2.0
├── repository.yml       # 仓库版本元数据
├── version.yml          # 版本号
├── Doxyfile             # Doxygen 配置
└── syscfg_all.h         # 汇总所有 syscfg 选项的头文件
```

---

## 4. 核心模块详解

### 4.1 Controller —— BLE 控制器层 (`nimble/controller/`)

Controller 实现了 Bluetooth Core Spec 中的 **Link Layer（链路层）** 和 HCI 控制端，是整个协议栈最靠近硬件的部分。

**所在路径**：[nimble/controller/src/](nimble/controller/src/)

| 源文件 | 功能 |
|---|---|
| `ble_ll.c` | Link Layer 主入口与核心状态机 |
| `ble_ll_adv.c` | 传统广播与扩展广播状态机 |
| `ble_ll_scan.c` / `ble_ll_scan_aux.c` | 扫描（主广播信道 + 辅助信道） |
| `ble_ll_conn.c` / `ble_ll_conn_hci.c` | 连接状态机与 HCI 连接命令 |
| `ble_ll_ctrl.c` | LL Control PDU 处理（连接控制信令） |
| `ble_ll_sync.c` | 周期性广播同步接收 |
| `ble_ll_iso.c` / `ble_ll_iso_big.c` | 同步信道（ISO / CIS / BIS，用于 LE Audio） |
| `ble_ll_isoal.c` | ISO 适配层（ISO over HCI） |
| `ble_ll_sched.c` | 调度器（时隙调度：广播/扫描/连接/ISO 冲突处理） |
| `ble_ll_rand.c` | 随机数生成 |
| `ble_ll_crypto.c` | LL 层加密（AES-CCM） |
| `ble_ll_cs.c` / `ble_ll_cs_drbg.c` | Channel Sounding 信道测距 |
| `ble_ll_resolv.c` | 可解析私有地址（RPA）解析列表 |
| `ble_ll_whitelist.c` | 白名单 |
| `ble_ll_rfmgmt.c` | RF 管理（功率、频偏校准等） |
| `ble_ll_pdu.c` | PDU 解析与构造 |
| `ble_ll_dtm.c` | 直接测试模式 |
| `ble_ll_hci.c` / `ble_ll_hci_ev.c` / `ble_ll_hci_supp_cmd.c` / `ble_ll_hci_vs.c` | HCI 控制器端：命令、事件、厂商扩展 |
| `ble_ll_utils.c` / `ble_ll_trace.c` | 工具函数与追踪日志 |

---

### 4.2 Host —— BLE 主机层 (`nimble/host/`)

Host 实现了蓝牙协议栈中控制器之上的所有高层协议。

**所在路径**：[nimble/host/src/](nimble/host/src/)

#### 4.2.1 Host 核心框架

| 源文件 | 功能 |
|---|---|
| `ble_hs.c` | Host 核心：启动、停止、主流程 |
| `ble_hs_cfg.c` | Host 运行时配置 |
| `ble_hs_conn.c` | Host 侧连接管理表 |
| `ble_hs_hci.c` / `ble_hs_hci_cmd.c` / `ble_hs_hci_evt.c` / `ble_hs_hci_util.c` | HCI 主机端：发送命令、处理事件 |
| `ble_hs_adv.c` | 广播数据解析与构造 |
| `ble_hs_id.c` | 本地身份地址管理（公网/静态随机/RPA） |
| `ble_hs_pvcy.c` | 隐私特性（RPA 地址轮换） |
| `ble_hs_flow.c` | HCI 流控（缓冲区管理） |
| `ble_hs_mbuf.c` / `ble_hs_mqueue.c` | 内存 mbuf 与消息队列封装 |
| `ble_hs_startup.c` / `ble_hs_stop.c` / `ble_hs_shutdown.c` | Host 启动/关闭流程 |
| `ble_hs_atomic.c` | 原子操作工具 |
| `ble_hs_misc.c` | 杂项工具 |
| `ble_hs_log.c` / `ble_hs_periodic_sync.c` | 日志与周期性广播同步 |

#### 4.2.2 协议模块

| 源文件 | 功能 |
|---|---|
| **GAP** | |
| `ble_gap.c` | 通用访问 Profile（GAP）API：广播、扫描、连接、参数更新 |
| **L2CAP** | |
| `ble_l2cap.c` | L2CAP 核心（固定信道、CID 路由、分片重组） |
| `ble_l2cap_sig.c` / `ble_l2cap_sig_cmd.c` | L2CAP 信令信道：连接参数更新、LE Credit Based |
| `ble_l2cap_coc.c` | L2CAP Connection Oriented Channel（面向连接的信道） |
| **ATT / GATT** | |
| `ble_att.c` | 属性协议（ATT）通用：超时、分片、MTU |
| `ble_att_svr.c` / `ble_att_clt.c` | ATT 服务端与客户端 |
| `ble_att_cmd.c` | ATT PDU 编解码（请求/响应/通知/指示） |
| `ble_gatts.c` / `ble_gatts_lcl.c` | GATT 服务端：本地属性数据库、读/写/通知回调 |
| `ble_gattc.c` | GATT 客户端：服务发现、读、写、订阅 |
| `ble_eatt.c` | Enhanced ATT（基于 L2CAP COC 的多信道 ATT） |
| **Security Manager** | |
| `ble_sm.c` | 安全管理器入口（配对/绑定/加密） |
| `ble_sm_cmd.c` | SMP PDU 编解码 |
| `ble_sm_alg.c` | SM 密码学算法（F4/F5/F6/G2/JW/c1/s1 等） |
| `ble_sm_lgcy.c` | Legacy 配对（Just Works / Passkey / OOB） |
| `ble_sm_sc.c` | Secure Connections 安全连接配对（ECDH + LE Secure Connections） |
| **其他** | |
| `ble_store.c` / `ble_store_util.c` | 键值存储抽象（绑定密钥、CCCD 等持久化） |
| `ble_uuid.c` | UUID 工具（16/32/128 位转换与比较） |
| `ble_eddystone.c` / `ble_ibeacon.c` | Eddystone / iBeacon 广播格式 |
| `ble_cs.c` | Host 侧 Channel Sounding API |
| `ble_dtm.c` | Host 侧 DTM 控制 |
| `ble_iso.c` | Host 侧 ISO（同步信道）管理 |

#### 4.2.3 GATT 标准服务 (`nimble/host/services/`)

每个子目录对应一个蓝牙 SIG 标准化的 GATT 服务：

| 服务 | 全称 |
|---|---|
| `gap/` | Generic Access Profile |
| `gatt/` | Generic Attribute Profile |
| `ans/` | Alert Notification Service（告警通知） |
| `bas/` | Battery Service（电池） |
| `dis/` | Device Information Service（设备信息） |
| `ias/` | Immediate Alert Service（立即告警） |
| `lls/` | Link Loss Service（链路丢失） |
| `tps/` | Tx Power Service（发射功率） |
| `ipss/` | Internet Protocol Support Service |
| `bleuart/` | Nordic UART Service（串口透传） |

另外还有 `store/` 目录——提供 `ble_store` 的默认 RAM 实现，`audio/` 目录（LE Audio 音频相关代码），`test/` 目录（Host 单测），`tools/` 辅助脚本，`util/` 工具。

---

### 4.3 Mesh —— 蓝牙 Mesh 子系统 (`nimble/host/mesh/`)

Mesh 是 Host 内部一个相对独立的子系统，实现了 Bluetooth Mesh Profile Specification（含 Mesh Model、配网、中继、代理、朋友节点、低功耗节点等）。

**所在路径**：[nimble/host/mesh/src/](nimble/host/mesh/src/)

#### Mesh 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                  Foundation Models                       │
│    Config Server/Client  │  Health Server/Client        │
├─────────────────────────────────────────────────────────┤
│                  Access Layer (access.c)                │
│    Model Publish/Subscribe / AppKey / Element / Opcode  │
├─────────────────────────────────────────────────────────┤
│          Upper Transport Layer (transport.c)            │
│     Segmentation / Reassembly / Access / Control PDU    │
├─────────────────────────────────────────────────────────┤
│          Lower Transport Layer (transport.c)            │
│     Friend / LPN Encryption / Replay Protection (RPL)   │
├─────────────────────────────────────────────────────────┤
│                  Network Layer (net.c)                  │
│     Relay / Network Encryption / Subnet / Sequence      │
├─────────────────────────────────────────────────────────┤
│                 Bearer Layer                            │
│   PB-ADV (adv.c) │ Adv Bearer │ GATT Bearer (pb_gatt)   │
├─────────────────────────────────────────────────────────┤
│             Provisioning Layer (prov.c)                 │
│  PB-ADV (pb_adv.c) │ PB-GATT (pb_gatt.c)               │
│  Device (prov_device.c) │ Provisioner (provisioner.c)   │
└─────────────────────────────────────────────────────────┘
```

| 源文件 | 功能 |
|---|---|
| **Bearer & 广播** | |
| `adv_legacy.c` / `adv_ext.c` / `adv.c` / `adv.h` | Mesh 广播承载（Legacy Adv + Extended Adv） |
| `beacon.c` / `beacon.h` | Mesh Beacon（未配网/安全网络信标） |
| `proxy_srv.c` / `proxy_msg.c` / `proxy.h` | GATT Proxy（PB-GATT 与 Proxy 协议承载） |
| **配网 Provisioning** | |
| `prov.c` / `prov.h` | 配网协议核心 |
| `prov_device.c` | 设备端（被配网） |
| `provisioner.c` / `provisioner.h` | Provisioner 端（发起配网） |
| `pb_adv.c` | PB-ADV 承载 |
| `pb_gatt.c` / `pb_gatt_srv.c` / `pb_gatt_srv.h` | PB-GATT 承载与 GATT 服务 |
| **Network Layer** | |
| `net.c` / `net.h` | 网络层：中继、NetKey 加密、TTL/SEQ |
| `subnet.c` / `subnet.h` | 子网管理（多个 NetKey、密钥刷新、IV Index） |
| `rpl.c` / `rpl.h` | Replay Protection List 重放保护 |
| **Transport Layer** | |
| `transport.c` / `transport.h` | 传输层（SAR 分段/重组、Friend/LPN、Heartbeat） |
| `friend.c` / `friend.h` | Friend 节点功能 |
| `lpn.c` / `lpn.h` | Low Power Node 低功耗节点 |
| `heartbeat.c` / `heartbeat.h` | 心跳（拓扑发现与监控） |
| **Access Layer & Model** | |
| `access.c` / `access.h` | 访问层：Element、Model、AppKey、Publish、订阅表 |
| `model_srv.c` / `model_cli.c` | 通用 Model Server/Client 框架 |
| `cfg.c` / `cfg_srv.c` / `cfg_cli.c` / `cfg.h` | Configuration Server / Client 模型 |
| `health_srv.c` / `health_cli.c` | Health Server / Client 模型 |
| `light_model.c` / `light_model.h` | 灯光模型示例（Generic OnOff/Level/Light Lightness 等） |
| **安全与密钥** | |
| `crypto.c` / `crypto.h` | Mesh 密码学抽象（AES-CCM / AES-CMAC / ECDH P-256） |
| `aes-ccm.c` | AES-CCM 加解密实现 |
| `app_keys.c` / `app_keys.h` | AppKey 与 NetKey 绑定管理 |
| **持久化与调试** | |
| `cdb.c` | Composition Data（设备组成数据） |
| `settings.c` / `settings.h` | 配置持久化（NV 存储接口） |
| `glue.c` | Mesh 与 NimBLE Host 之间的适配粘合代码 |
| `shell.c` / `shell.h` | Mesh Shell CLI 命令 |
| `mesh.c` / `mesh_priv.h` | Mesh 子系统初始化与内部私有头 |
| `msg.c` | 消息收发辅助 |
| `atomic.h` | 原子操作（Mesh 专用） |
| `foundation.h` | Foundation Model 公共定义 |
| `testing.c` / `testing.h` | 测试辅助接口 |

> **用户当前打开的文件**：[model_srv.c](nimble/host/mesh/src/model_srv.c) 是 Mesh Model Server 的通用框架，定义了 Mesh Model 操作码（Opcode）处理表、消息回调、发布/订阅等通用 Server 侧逻辑。

---

### 4.4 Transport —— 传输层 (`nimble/transport/`)

负责 Host 和 Controller 之间的 HCI 数据传输。当 Host 与 Controller 在同一颗 MCU 上时走 RAM / shared-memory；分离时走 UART、USB、Socket 等。

| 子目录 | 传输方式 | 适用场景 |
|---|---|---|
| `uart/` | UART H4 协议 | 经典的 Host ↔ Controller 串口连接（与 BlueZ、Zephyr 等对接） |
| `uart_ll/` | LL 直接串口 | 链路层直通串口（调试/测试） |
| `socket/` | TCP/UDP Socket | Linux 仿真 / 与 PC 工具对接 |
| `usb/` | USB（CDC/HCI） | nRF52840 等带 USB 的芯片，作为 USB dongle |
| `emspi/` | emSPI（Enhanced SPI） | Nordic 等芯片使用的高速 SPI HCI |
| `dialog_cmac/` | Dialog CMAC 邮箱 | Renesas DA1469x CMAC 核间通信 |
| `nrf5340/` | nRF5340 IPC | Nordic nRF5340 网络核与应用核之间 |
| `apollo3/` | Ambiq Apollo3 | 特定平台传输 |
| `cdc/` | CDC（通用） | 通用 USB CDC 承载 |
| `common/` / `include/` / `src/` | 公共代码与 HCI 适配层 | 传输层抽象接口 |

---

### 4.5 Drivers —— 射频驱动 (`nimble/drivers/`)

对不同射频芯片（无线 SoC）的封装，Controller 调用这些驱动操作射频寄存器。

| 子目录 | 芯片 |
|---|---|
| `nrf51/` | Nordic nRF51 系列（nRF51822） |
| `nrf52/` | Nordic nRF52 系列（nRF52805/810/820/832/833/840） |
| `nrf5340/` | Nordic nRF5340（网络核） |
| `nrf5x/` | nRF5x 公共代码（时钟、RTC、定时器等） |
| `dialog_cmac/` | Renesas DA1469x（CMAC 加速器） |
| `native/` | 原生仿真驱动（用于 `native` babblesim 仿真） |
| `fem/` | 前端放大模块（PA/LNA 芯片支持） |

---

### 4.6 Porting Layer —— 移植层 (`porting/`)

NimBLE 设计为**跨平台**的协议栈，通过 NPL（NimBLE Porting Layer）抽象操作系统 API。

#### 4.6.1 NPL (`porting/npl/<os>/`)

| 实现 | 目标系统 |
|---|---|
| `mynewt/` | Apache Mynewt OS（原生 OS） |
| `linux/` | Linux（用户态 pthreads + timerfd） |
| `freertos/` | FreeRTOS（ESP-IDF 等使用） |
| `nuttx/` | Apache NuttX RTOS |
| `riot/` | RIOT OS |
| `dummy/` | 空实现（做编译验证用） |

NPL 抽象的接口类型：
- 内存分配（`os_malloc`/`os_free`）
- 内存池 `os_mempool`
- 缓冲区 `os_mbuf`（网络 mbuf 链式缓冲区）
- 事件队列 `os_eventq`
- 定时器 `os_callout` / CPU 时间 `os_cputime`
- 互斥、信号量
- 任务/线程

#### 4.6.2 `porting/nimble/`

通用移植代码（`src/`、`include/`）和 Makefile 片段（`Makefile.defs`、`Makefile.controller`、`Makefile.mesh`、`Makefile.mbedtls`）。

- `src/nimble_port.c`：NimBLE Port 入口初始化
- `src/os_mbuf.c` / `src/os_mempool.c`：mbuf/mempool 默认实现
- `src/os_cputime.c` / `os_cputime_pwr2.c`：CPU 时间与定时器
- `src/mem.c`：堆分配封装
- `src/endian.c`：大小端工具
- `src/hal_timer.c`：HAL 定时器抽象
- `mbedtls_user_config.h`：Mbed TLS 的 NimBLE 裁剪配置

---

### 4.7 Applications —— 示例应用 (`apps/`)

| 应用 | 描述 |
|---|---|
| `bleprph` | **最小 BLE 外设**（GATT Server，常用于入门） |
| `blecent` | **中心设备**：扫描、连接、读写、订阅 |
| `blehci` | **纯 Controller**：HCI over UART，可对接 Linux BlueZ |
| `btshell` | **CLI Shell**：通过命令行可操作几乎所有 BLE 功能 |
| `blehr` | 心率服务（Heart Rate）外设 |
| `blecsc` | 骑行速度/踏频（Cycling Speed and Cadence） |
| `blemesh` | 基础 Mesh 节点 |
| `blemesh_shell` | Mesh CLI Shell（通过串口操作配网、模型等） |
| `blemesh_light` | Mesh 灯光模型示例（OnOff / Lightness / HSL 等） |
| `blemesh_models_example_1/2` | Mesh 模型开发示例 |
| `auracast` | LE Audio Auracast 广播示例 |
| `advertiser` / `ext_advertiser` / `scanner` | 广播/扩展广播/扫描的极简示例 |
| `peripheral` / `central` | 通用外设/中心 |
| `blestress` | 压力测试 |
| `dtm` | 直接测试模式（RF 认证） |
| `bttester` | 自动化测试服务端 |
| `mesh_badge` | Mesh 展会徽章演示 |

---

## 5. 关键数据流

### 5.1 BLE 连接通信（GATT Write）

```
App → ble_gatts 注册 attr cb
        ↓
Peer 发送 Write 数据包
        ↓
  Radio → driver → ble_ll 接收 → ble_ll_hci → HCI ACL
        ↓
Transport（RAM / UART / Socket 等）
        ↓
ble_hs_hci_evt 解析 HCI ACL → ble_l2cap 路由到 ATT CID
        ↓
ble_att_svr → 查属性表 → 调用用户注册的 ble_gatt_attr 回调
        ↓
返回 Write Response → 原路返回
```

### 5.2 Mesh 消息中继（Relay）

```
Radio 收到 Mesh Adv 报文
    ↓
mesh/adv_legacy.c (bearer) → 去重
    ↓
net.c (Network Layer)：解密 NetKey → 检查 SEQ/RPL → TTL-- → 判断是否转发
    ↓ （如果是给自己的）
transport.c (Lower + Upper Transport)：解密 TransKey → 分片重组
    ↓
access.c (Access Layer)：匹配 Element/Model/Opcode → AppKey 解密
    ↓
model_srv.c → 调用对应 Model 的 Opcode 回调（如 light_model Generic OnOff Set）
    ↓ （如果需要回复）
原路打包 → 封装 Access/Transport/Network/Bearer → 广播出去
```

### 5.3 Host + Controller 通信（HCI）

```
ble_hs_hci_cmd.c → 构造 HCI Command
    ↓
transport/src/ 选择具体传输后端
    ↓
Transport: UART (H4: 0x01 + Opcode + Len + Param)
    ↓
UART 线 / RAM / USB / Socket 等
    ↓
controller/src/ble_ll_hci.c → 解析命令 → 执行（ble_ll_adv / ble_ll_conn / ...）
    ↓
构造 HCI Event → 原路返回 Host
    ↓
ble_hs_hci_evt.c → 分发事件（LE Adv Report / Disconnect Complete / ...）
```

---

## 6. 构建与配置系统

### 6.1 两种构建方式

| 方式 | 配置 | 适用 |
|---|---|---|
| **Apache Newt** | `pkg.yml` + `syscfg.yml` | Mynewt 生态原始构建系统，配合 `targets/` 目录 |
| **Makefile** | `porting/nimble/Makefile.*` | 在 Linux、NuttX、RIOT 等外部项目中直接集成 NimBLE |

### 6.2 关键配置文件

- [syscfg.yml](nimble/syscfg.yml) / [host/syscfg.yml](nimble/host/syscfg.yml) / [controller/syscfg.yml](nimble/controller/syscfg.yml) / [transport/syscfg.yml](nimble/transport/syscfg.yml)：各子模块的配置项（连接数、MTU、内存池大小、是否启用 Mesh/ISO 等），编译期通过 `sysinit` 系统初始化。
- `syscfg_all.h`：汇总所有配置项为宏（便于外部项目 include）。
- `pkg.yml`：Newt 构建系统的包依赖与源文件列表。
- `repository.yml` / `version.yml`：仓库版本与 Newt 兼容性声明。

### 6.3 CI/CD

`.github/workflows/` 中定义了 GitHub Actions：
- `build_targets.yml`：所有硬件构建目标
- `build_cc_target.yml`：CC 编译选项验证
- `build_ports.yml`：所有 OS 移植构建
- `newt_test_all.yml`：所有 newt 单元测试
- `build_all_apps.yml`：所有 apps 编译

---

## 7. 支持的硬件与平台

### 7.1 Controller 硬件

| 芯片/平台 | 驱动目录 | 厂商 |
|---|---|---|
| Nordic nRF51 系列 | `drivers/nrf51` | Nordic Semiconductor |
| Nordic nRF52 系列 (nRF52832/840 等) | `drivers/nrf52` | Nordic Semiconductor |
| Nordic nRF5340（网络核） | `drivers/nrf5340` | Nordic Semiconductor |
| Renesas DA1469x (CMAC) | `drivers/dialog_cmac` | Renesas (原 Dialog) |

### 7.2 Host 运行平台

Host 不绑定硬件，只要 OS 实现了 NPL 即可：
- Apache Mynewt OS
- Linux（用户态）
- FreeRTOS（ESP-IDF 等）
- Apache NuttX
- RIOT OS
- Open IoT SDK（CMSIS RTOSv2：Amazon FreeRTOS / CMSIS-RTX / Azure ThreadX）

### 7.3 外部集成项目

- **Espressif ESP-IDF**：ESP32 系列内置 NimBLE 支持
- **RIOT OS**：通过 `pkg/nimble` 包
- **Open IoT SDK**：Arm IoT SDK

---

## 8. 外部依赖 (`ext/`)

| 目录 | 作用 |
|---|---|
| `ext/tinycrypt/` | 轻量级对称加密（AES、CMAC、CCM），用于嵌入式缺加密库的环境 |
| `ext/liblc3/` | LC3 低复杂度通信编解码器（LE Audio 使用） |
| `ext/libsamplerate/` | 音频采样率转换（LE Audio 采样率适配） |

Mbed TLS（ECC 与 SHA）则由目标系统提供或由 NPL 集成，`porting/nimble/Makefile.mbedtls` 给出了集成方式与裁剪头 `mbedtls_user_config.h`。

---

## 9. 文档与学习资源建议

1. **入门**：从 [README.md](README.md) 开始，编译运行 `apps/bleprph` 或 `apps/btshell`。
2. **Host API**：头文件位于 `nimble/host/include/`，常用：
   - `nimble/ble.h`、`host/ble_gap.h`、`host/ble_gatt.h`、`host/ble_hs.h`、`host/ble_l2cap.h`、`host/ble_sm.h`。
3. **Mesh 开发**：
   - 头文件：`nimble/host/mesh/include/mesh/` 下的 `mesh.h`、`access.h`、`provisioner.h` 等。
   - 示例：`apps/blemesh_light` 与 `apps/blemesh_shell`。
   - 文档：`docs/mesh/`。
4. **移植到新 OS**：参考 `porting/npl/linux` 与 `porting/npl/freertos` 实现 NPL 接口；`porting/examples/linux` 给出完整工程范例。
5. **代码规范**：[CODING_STANDARDS.md](CODING_STANDARDS.md)。
6. **单元测试**：各模块 `test/` 目录，由 Newt 构建系统运行。

---

## 10. 与你当前关注的文件

你当前打开的 [model_srv.c](nimble/host/mesh/src/model_srv.c#L211) 属于 **Mesh Access Layer 下的 Model Server 框架**。它的作用是：
- 为所有 Mesh Model Server 提供统一的消息派发、Opcode 处理表（`BT_MESH_MODEL_OP_ENTRY / BT_MESH_MODEL_OP_END`）、发布/订阅、步限（Step/Publish Period）等通用能力；
- 上层的灯光模型（`light_model.c`）、配置服务器（`cfg_srv.c`）、健康服务器（`health_srv.c`）都通过它注册 Opcode 回调。

如果需要深入 Mesh 开发，建议与 `access.c`、`transport.c`、`light_model.c`、`cfg_srv.c` 一起阅读，它们构成了 Mesh Model 开发的主干链路。
